-
  const social = [
    {
      'icon': 'vk',
      'link': 'https://vk.com/'
    }, {
      'icon': 'ig',
      'link': 'https://www.instagram.com/'
    }, {
      'icon': 'ta',
      'link': 'https://www.tripadvisor.com/'
    }
  ]
-
  const contacts = [
    {
      'icon': 'tel',
      'tel': ''
    }, {
      'icon': 'at',
      'email': appData.meta.email,
      'subject': ''
    }
  ]

footer.page__footer
  ul
    //- li.copy= `© ${new Date().getFullYear()}, ООО «${appData.meta.name}»`
    li.copy= `© ${new Date().getFullYear()}`
    li.logo(itemprop="name")= appData.meta.name

  ul.social
    each item in social
      li
        a(href= item.link target="_blank" rel="noopener noreferrer")
          span(data-icon= item.icon)
  ul
    each item in contacts
      - let inner = item.tel || item.email;
      li
        a(
          href=`${item.tel ? 'tel' : 'mailto'}:${inner}`
          target="_blank" rel="noopener noreferrer"
        )
          span(data-icon= item.icon)

  span.byline
    | Сделано в «RMMR Studio»
