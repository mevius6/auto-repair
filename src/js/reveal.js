import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import { selectAll } from './utils';

const revealEffect = (el) => {
  if (el.tagName === 'IMG') {
    //
  }
  if (el.tagName === 'P' || 'H1' || 'H2' || 'H3') {
    //
  }
  if (el.tagName === 'ARTICLE' || 'SECTION') {
    //
  }
}
